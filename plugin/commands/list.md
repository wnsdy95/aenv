---
description: List all aenv environments
allowed-tools: Bash
---

Run `aenv list -l` to show available environments with their paths and
descriptions. The currently active env is marked with `*`.

```
!aenv list -l
```
